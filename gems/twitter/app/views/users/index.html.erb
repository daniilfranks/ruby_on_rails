<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= paginate @users %>

<% @users.each do |user| %>
  <p><%= gravatar_for(user) %></p>
  <p><%= link_to user.name, user %></p>
<% end %>

<%= paginate @users %>
